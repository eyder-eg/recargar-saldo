import { css } from "lit";

export default css`
  .card {
    background-color: #edf2ff;
    display: flex;
    padding: 1rem 1rem;
    gap: 0.5rem;
    border-radius: 1rem;
  }

  .container-text {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 0.2rem;
  }
`;
